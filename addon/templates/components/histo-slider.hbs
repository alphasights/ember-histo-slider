<svg class="ember-histo-slider__histogram" width={{svgWidth}} height={{svgHeight}}></svg>
<div class="ember-histo-slider__slider" style="width: {{histogramWidth}}px; margin-left: {{margin.left}}px;">
  {{range-slider
    connect="upper"
    on-update="updateValue"
    on-change="setValue"
    min=dataMin
    max=dataMax
    start=startValue}}

    {{value}}
    {{setValue}}
</div>
