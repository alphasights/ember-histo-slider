<svg class={{concat "ember-histo-slider__histogram " uniqueHistoSliderId}} width={{svgWidth}} height={{svgHeight}}>
  <g transform="translate({{margin.left}},{{margin.top}})">
    <g transform="scale(1,1)">
      {{#each bins as |bin index|}}
        <g class="bar" transform="translate({{object-at index rectXs}},{{histogramHeight}}) scale(-1,1)">
          <rect class="
            ember-histo-slider__rect
            {{if (lte index currentBinIndex) 'ember-histo-slider__rect--excluded'}}"
            transform="rotate(180)"
            width={{rectWidth}}
            height="{{object-at index rectHeights}}px"/>
        </g>
      {{/each}}
    </g>
  </g>
</svg>
<div class="ember-histo-slider__slider" style={{histoStyle}}>
  {{range-slider
    connect="upper"
    on-update="updateValue"
    on-change="setValue"
    min=dataMin
    max=dataMax
    start=(object-at 0 startValue)}}

    {{value}}
    {{setValue}}
</div>
